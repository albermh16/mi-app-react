# Conexión a MongoDB
MONGODB_URI=

# JWT secrets (pon cadenas largas en tu .env real)
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
JWT_ACTIVATION_SECRET=

# URLs
API_URL=http://localhost:3000
APP_URL=http://localhost:5173

# SMTP (correo)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=
